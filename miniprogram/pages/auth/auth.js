// pages/auth/auth.js
import regeneratorRuntime, { async } from '../../lib/runtime/runtime';
import request from '../../network/request'
Page({

    async handlegetUserInfo(e) {
        // console.log(e);
        // const { encryptedData, rawData, rawData, iv } = e.detail
        // 
        // wx.login({
        //     success: function(res) {
        //         // console.log(res)
        //         // const { code } = res
        //     }
        // })
        // 发送请求
        // const login = { encryptedData, rawData, rawData, iv}
        // const res = await request({url:"/users/wxlogin",data:login,method:"post"})
        // const { token } = res
        // wx.setStorageSync('token', token)
        // wx.navigateBack({
        //     delta: 1
        // })

    }
})